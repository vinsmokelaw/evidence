import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Evidence Tanaka Murima - UX/UI & Graphic Designer',
  description: 'Creative and certified UX/UI & Graphic Designer based in Zimbabwe. Specializing in innovative design solutions that engage audiences and drive results.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
